<h1>トップページ</h1>
<%= link_to '新規投稿', '/posts/new' %>

<% @post.each do |post| %>
  <div class="post">
    <div class="post-content">
      <%= post.content %>
    </div>
    <div class="post-date">
      <%= post.created_at %>
    </div>
  </div>

<% end %>

  
      